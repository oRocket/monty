#include "monty.h"

/**
 * f_nop - Does nothing.
 * @head: Double pointer to the stack (head).
 * @counter: The line number in the file (unused).
 *
 * This function is a no-operation (nop) instruction. It does nothing and
 * serves as a placeholder for specific operations in the Monty language.
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
